最近我们在预研一个极简的自研虚拟机，为了给团队演示多线程下的内存回收卡顿效应，我需要你用纯 Python 帮我从零写一个带可视化界面的垃圾回收 STW（Stop-The-World）仿真内核。代码必须是一个单文件闭环，严禁引入任何第三方依赖，只能用原生标准库，前端页面仅限原生 HTML 和 Canvas 并通过内置 HTTP 服务渲染。你需要实现一个并发环境下的对象分配与图遍历标记-清除算法。

后台会有应用线程随机分配内存并修改对象引用图，同时有 GC 线程在后台定期扫描。难点在于必须严格处理并发图遍历的读写竞态，并且在触发 STW 清理瞬间，必须安全地挂起所有应用线程。前端页面要和后台高频并发解耦，流畅地在网格中实时渲染出存活对象、垃圾对象以及 GC 扫描光标的游走轨迹，让我能肉眼看到 STW 瞬间整个网格分配停滞的视觉冲击，直接交给我一套无并发死锁的可运行源码，和一键运行脚本。
